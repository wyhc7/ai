{
  "name": "ai-gateway",
  "version": "1.0.0",
  "description": "AI 中转站：多平台多 Key 管理 + 自动故障切换 + 可视化配置",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "start": "bash start.sh"
  }
}
